import React from "react";
import styles from "./Hero.module.scss";

const Hero = () => {
  return (
    <section className={styles.hero}>
      <h1>Hero</h1>
    </section>
  );
};

export default Hero;
